<Window x:Class="CheckpointApp.Views.EditPersonWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CheckpointApp.Views"
        mc:Ignorable="d"
        Title="Редактирование личных данных" Height="450" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontSize="16" FontWeight="Bold" Margin="0,0,0,15">
            <Run Text="Редактирование досье для паспорта:"/>
            <Run Text="{Binding PersonToEdit.PassportData}" Foreground="DarkBlue"/>
        </TextBlock>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Grid.Column="0" Content="Фамилия:" VerticalAlignment="Center"/>
            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding PersonToEdit.LastName}" Margin="5" VerticalAlignment="Center"/>

            <Label Grid.Row="1" Grid.Column="0" Content="Имя:" VerticalAlignment="Center"/>
            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding PersonToEdit.FirstName}" Margin="5" VerticalAlignment="Center"/>

            <Label Grid.Row="2" Grid.Column="0" Content="Отчество:" VerticalAlignment="Center"/>
            <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding PersonToEdit.Patronymic}" Margin="5" VerticalAlignment="Center"/>

            <Label Grid.Row="3" Grid.Column="0" Content="Дата рождения:" VerticalAlignment="Center"/>
            <DatePicker Grid.Row="3" Grid.Column="1" SelectedDate="{Binding PersonDob}" Margin="5" VerticalAlignment="Center"/>

            <Label Grid.Row="4" Grid.Column="0" Content="Гражданство:" VerticalAlignment="Center"/>
            <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding PersonToEdit.Citizenship}" Margin="5" VerticalAlignment="Center"/>

            <Label Grid.Row="5" Grid.Column="0" Content="Примечания:" VerticalAlignment="Top" Margin="0,5,0,0"/>
            <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding PersonToEdit.Notes}" Margin="5" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Сохранить" Width="120" IsDefault="True" Command="{Binding SaveChangesCommand}" Margin="5"/>
            <Button Content="Отмена" Width="100" IsCancel="True" Margin="5"/>
        </StackPanel>

    </Grid>
</Window>
